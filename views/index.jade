extends layout

block content

  h1= title
  p Welcome to #{title}

  span(id="note")

  form#create-new-note(action="hehe")
    input(type="text" placeholder="Type your secret...")
    input(type="submit")
